import { createContext } from "react";

export const DrawStateContext = createContext(undefined);
export const SetDrawStateContext = createContext(undefined);
export const LayersContext = createContext(undefined)
export const SetLayersContext = createContext(undefined);